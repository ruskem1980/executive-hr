# JSON Translation Tools (TypeScript/JavaScript Projects)

Инструменты для валидации и тестирования JSON-переводов в TypeScript/JavaScript проектах (Next.js, React, Vue и т.д.)

Применяет правила **Platinum Standard** к JSON-переводам с вложенными ключами (dot-notation).

---

## 🛠️ Инструменты

### 1. json_validator.py
**Валидатор JSON-переводов с правилами PT_Standart**

Проверяет:
- ✅ **Полнота переводов** — все ключи из эталонного языка есть в других
- ✅ **Плейсхолдеры** — `{{var}}` совпадают между языками
- ✅ **Аномалии длины** — перевод не отличается >200% от эталона
- ✅ **Пустые значения** — нет пустых строк
- ✅ **Смешанные алфавиты** — кириллица в английском и т.д.

**Использование:**
```bash
# Базовое использование
python -m src.i18n.json_validator --locales-dir /path/to/locales --reference ru

# Next.js проект
python -m src.i18n.json_validator \
  --locales-dir ./apps/frontend/src/locales \
  --reference ru

# С кастомными лимитами вывода
python -m src.i18n.json_validator \
  --locales-dir ./locales \
  --reference en \
  --max-errors 100 \
  --max-warnings 50
```

**Параметры:**
- `--locales-dir` (обязательный) — путь к директории с JSON файлами
- `--reference` (опционально) — эталонный язык (по умолчанию: `ru`)
- `--max-errors` (опционально) — максимум ошибок для вывода (по умолчанию: 50)
- `--max-warnings` (опционально) — максимум предупреждений для вывода (по умолчанию: 30)

**Пример вывода:**
```
================================================================================
ЗАПУСК ВАЛИДАЦИИ ПЕРЕВОДОВ (Platinum Standard Rules)
================================================================================

Загружено языков: en, ky, ru, tg, uz
Эталонный язык: ru
  - en: 2821 ключей
  - ky: 2747 ключей
  - ru: 2821 ключей
  - tg: 2747 ключей
  - uz: 2760 ключей

Проверка 1/5: Полнота переводов...
Проверка 2/5: Плейсхолдеры {{var}}...
Проверка 3/5: Аномалии длины...
Проверка 4/5: Пустые значения...
Проверка 5/5: Смешанные алфавиты...

================================================================================
ОТЧЁТ ПО ВАЛИДАЦИИ
================================================================================

Всего проблем: 260
  - Ошибки (ERROR): 216
  - Предупреждения (WARNING): 44

ОШИБКИ (ERROR):
--------------------------------------------------------------------------------
1. [tg] risk.action.consultLawyer
   → Отсутствует перевод (есть в ru, нет в tg)

СТАТИСТИКА ПО ЯЗЫКАМ:
--------------------------------------------------------------------------------
✅ en: 2821 ключей | 0 ошибок | 20 предупреждений
❌ ky: 2747 ключей | 74 ошибок | 10 предупреждений

================================================================================
❌ ВАЛИДАЦИЯ НЕ ПРОЙДЕНА: 216 ошибок
================================================================================
```

**Exit codes:**
- `0` — валидация пройдена (0 ошибок)
- `1` — валидация не пройдена (есть ошибки)

---

### 2. language_tester.py
**Тестер функции переключения языка**

Симулирует работу translation system на Python и проверяет:
- ✅ Загрузку всех языков
- ✅ Резолв ключей через dot-notation (`auth.phone.title`)
- ✅ Fallback на эталонный язык при отсутствии ключа
- ✅ Интерполяцию плейсхолдеров `{{var}}`

**Использование:**
```bash
# Базовое использование
python -m src.i18n.language_tester --locales-dir /path/to/locales --reference ru

# Next.js проект
python -m src.i18n.language_tester \
  --locales-dir ./apps/frontend/src/locales \
  --reference ru

# С английским как эталон
python -m src.i18n.language_tester \
  --locales-dir ./locales \
  --reference en
```

**Параметры:**
- `--locales-dir` (обязательный) — путь к директории с JSON файлами
- `--reference` (опционально) — эталонный язык / fallback (по умолчанию: `ru`)

**Пример вывода:**
```
================================================================================
ТЕСТИРОВАНИЕ ФУНКЦИИ ПЕРЕКЛЮЧЕНИЯ ЯЗЫКА
================================================================================

📦 Загружено языков: en, ky, ru, tg, uz
🔄 Текущий язык: ru

ТЕСТ 1: Базовые ключи на ru
--------------------------------------------------------------------------------
  [ru] auth.phone.title → 'Войти по номеру'
  [ru] auth.phone.subtitle → 'Введите номер телефона для получения кода'
  [ru] common.continue → 'Продолжить'

ТЕСТ 2: Переключение на en
--------------------------------------------------------------------------------
✅ Язык переключён на: en
  [en] auth.phone.title → 'Sign in by phone'
  [en] common.continue → 'Continue'

ТЕСТ 3: Fallback на ru (если ключ отсутствует)
--------------------------------------------------------------------------------
  ⚠️  Fallback: 'risk.title' не найден в uz, взят из ru
  [uz] risk.title → 'Оценка рисков'

ТЕСТ 4: Интерполяция плейсхолдеров {{var}}
--------------------------------------------------------------------------------
  🔍 Найден плейсхолдер в 'auth.code.subtitle': Мы отправили код на {{phoneNumber}}
  [ru] auth.code.subtitle → 'Мы отправили код на +7 999 123-45-67'

================================================================================
ИТОГИ ТЕСТИРОВАНИЯ
================================================================================

✅ Все языки загружены корректно
✅ Функция set_language() работает
✅ Функция t() резолвит ключи через dot-notation
✅ Fallback на ru работает для отсутствующих ключей
✅ Интерполяция {{var}} работает

================================================================================
```

---

## 📁 Структура JSON-переводов

Инструменты работают с JSON-файлами следующей структуры:

```
locales/
  ├── ru.json     # Эталонный язык
  ├── en.json
  ├── uz.json
  ├── tg.json
  └── ky.json
```

**Формат JSON (вложенные ключи с dot-notation):**
```json
{
  "auth": {
    "phone": {
      "title": "Войти по номеру",
      "subtitle": "Введите номер телефона"
    },
    "code": {
      "title": "Код подтверждения",
      "subtitle": "Мы отправили код на {{phoneNumber}}"
    }
  },
  "common": {
    "continue": "Продолжить",
    "cancel": "Отмена"
  }
}
```

**Использование в коде (TypeScript):**
```typescript
import { t } from '@/lib/i18n/translations'

// Dot-notation доступ
t('auth.phone.title')  // "Войти по номеру"

// С интерполяцией
t('auth.code.subtitle', { phoneNumber: '+7 999 123-45-67' })
// "Мы отправили код на +7 999 123-45-67"
```

---

## 🔄 Интеграция в CI/CD

### GitHub Actions

```yaml
name: Validate Translations

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  validate-i18n:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Validate JSON translations
        run: |
          python -m src.i18n.json_validator \
            --locales-dir ./apps/frontend/src/locales \
            --reference ru
```

### Pre-commit hook

```bash
# .husky/pre-commit
#!/bin/sh

# Валидация переводов перед коммитом
python -m src.i18n.json_validator \
  --locales-dir ./apps/frontend/src/locales \
  --reference ru

if [ $? -ne 0 ]; then
  echo "❌ Валидация переводов не пройдена. Исправьте ошибки перед коммитом."
  exit 1
fi
```

---

## 🆚 Отличия от основных инструментов PT_Standart

| Инструмент | Целевая аудитория | Формат |
|-----------|------------------|--------|
| `validator.py` | Python-проекты | TranslationEntry (внутренний формат) |
| `scanner.py` | Python-проекты | AST (Python код) |
| **`json_validator.py`** | **TypeScript/JS проекты** | **JSON (вложенные ключи)** |
| **`language_tester.py`** | **TypeScript/JS проекты** | **JSON (вложенные ключи)** |

---

## 💡 Примеры использования

### 1. Валидация Next.js проекта
```bash
cd /path/to/nextjs-project
python -m src.i18n.json_validator \
  --locales-dir ./src/locales \
  --reference ru
```

### 2. Валидация React приложения с английским эталоном
```bash
cd /path/to/react-app
python -m src.i18n.json_validator \
  --locales-dir ./public/locales \
  --reference en
```

### 3. Тестирование после добавления новых ключей
```bash
# 1. Валидация
python -m src.i18n.json_validator --locales-dir ./locales --reference ru

# 2. Тестирование работы
python -m src.i18n.language_tester --locales-dir ./locales --reference ru
```

---

## 🐛 Типичные проблемы

### Проблема: "Эталонный язык не найден"
```
ValueError: Эталонный язык 'ru' не найден. Доступны: ['en', 'fr']
```

**Решение:** Убедитесь, что файл `ru.json` существует в директории локалей или укажите правильный эталонный язык через `--reference en`.

### Проблема: "JSON файлы не найдены"
```
FileNotFoundError: JSON файлы не найдены в /path/to/locales
```

**Решение:** Проверьте путь к директории. Путь должен быть корректным и содержать `.json` файлы.

### Проблема: Аномалии длины (WARNING)
```
WARNING: Аномальная длина: 3 (ru) vs 26 (en) [ratio=8.67]
```

**Решение:** Это нормально для языков с разной структурой. Например:
- `"РВП"` (3 символа) → `"Temporary Residence Permit"` (26 символов)
- Предупреждение можно игнорировать, если перевод корректен.

---

## 📊 Отчёты

После валидации вы получите подробный отчёт:
- **Критические ошибки (ERROR)** — отсутствующие переводы, несовпадение плейсхолдеров
- **Предупреждения (WARNING)** — аномалии длины, лишние ключи
- **Статистика по языкам** — количество ключей и проблем для каждого языка

---

## 🚀 Быстрый старт

```bash
# 1. Клонировать PT_Standart
git clone https://github.com/your-org/PT_Standart.git
cd PT_Standart

# 2. Запустить валидацию на вашем проекте
python -m src.i18n.json_validator \
  --locales-dir /path/to/your/project/locales \
  --reference ru

# 3. Запустить тестирование
python -m src.i18n.language_tester \
  --locales-dir /path/to/your/project/locales \
  --reference ru
```

---

## 📝 Лицензия

Часть проекта **Platinum Standard**. См. LICENSE в корне репозитория.
